server:
  port: 8084
  servlet:
    context-path: /

spring:
  datasource:
    url: "jdbc:mysql://localhost:3304/beststore"
    driverClassName: "com.mysql.cj.jdbc.Driver"
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true